"""Opencodon's version metadata.

Single source of truth — ``opencodon.frontends.cli`` re-exports these for
existing consumers.  Keep ``__version__`` in sync with ``version`` in
pyproject.toml.
"""

# Opencodon's own version line — independent of the upstream fork point.
__version__ = "0.1.0"
__release_date__ = "2026.7.25"
